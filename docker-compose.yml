services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: vaultlite
      POSTGRES_USER: vaultlite_app
      POSTGRES_PASSWORD: vaultlite_app
    ports:
      - "5432:5432"
    volumes:
      - vaultlite_pg:/var/lib/postgresql/data
volumes:
  vaultlite_pg:
